{
  "name": "lambda-at-edge-cognito-authorizer",
  "version": "1.0.0",
  "private": true,
  "main": "src/index.ts",
  "dependencies": {
    "cognito-at-edge": "^1.5.4"
  },
  "devDependencies": {
    "@aws-sdk/client-secrets-manager": "^3.952.0",
    "@types/aws-lambda": "^8.10.159",
    "@types/node": "^25.0.2",
    "esbuild": "^0.27.1"
  },
  "scripts": {
    "build": "esbuild src/index.ts --bundle --minify --platform=node --target=node20 --outdir=dist"
  }
}
