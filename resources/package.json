{
  "name": "lambda-at-edge-cognito-authorizer",
  "version": "1.0.0",
  "private": true,
  "main": "src/index.ts",
  "dependencies": {
    "cognito-at-edge": "^1.5.4"
  },
  "devDependencies": {
    "@aws-sdk/client-secrets-manager": "^3.958.0",
    "@types/aws-lambda": "^8.10.159",
    "@types/node": "^25.0.3",
    "esbuild": "^0.27.2"
  },
  "scripts": {
    "build": "esbuild src/index.ts --bundle --minify --platform=node --target=node24 --outdir=dist"
  }
}
